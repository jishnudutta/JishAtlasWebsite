# docker compose reads this file automatically (copy to .env to use it).
#
# Host port the JishAtlas website is exposed on.
#   - Leave unset / default (8080): safe when something else already uses port 80
#     on this server (nginx, another site, etc.).
#   - Set to 80 for a plain deployment reachable at http://<server-ip>/
PORT=8080
